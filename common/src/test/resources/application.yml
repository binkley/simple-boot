headers:
  correlation-id:
    client:
      paths: /correlated
    server:
      paths: /correlated

logging:
  level: WARN

security:
  basic:
    enabled: false

spring:
  main:
    show-banner: false
